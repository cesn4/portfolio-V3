import React, { FunctionComponent } from 'react';

import './LogIn.scss';

const LogIn: FunctionComponent = () => {
    const className = 'log-in';
    return (
        <div className={className}>Hello there</div>
    );
};

export default LogIn;
